# FlowPDF 文档编辑模式手动验收

环境记录：Windows 版本、显示缩放、Python/打包版本、测试文件路径和结果都应填写。测试文件只使用
无敏感内容的本地副本；不要用唯一原件。

## 场景一：普通报告

1. 打开单栏两页报告，等待文档分析完成。
2. 在模式对话框中确认质量评分，选择“像 Word 一样编辑”。
3. 确认中央区域显示分离的白色纸张、页间空隙和阴影，左侧“文档页”出现实际缩略图。
4. 分别点击“适应页面”和“适应宽度”，确认整张纸或纸张宽度进入可视区域且图文比例不变。
5. 点击第一段中间，确认出现文本光标；双击应选词，连续三击应选中整段。
6. 输入一整句话，确认后续段落自动下移。
7. 继续输入至少 200 个中文字符，直到内容进入下一页；确认总页数和缩略图自动更新，光标始终留在视口中。
8. 从第一页拖动选择到第二页，在视口底边停留，确认画布自动向下滚动且选区跨过页间空隙后仍连续。
9. 删除刚输入的内容，确认后续段落自动上移，多余的末页自动消失。
10. 选择文字，修改字体、字号、粗体和行距；确认无选区时格式作用于后续输入。
11. 插入一个分页符，确认正文只进入下一页且不产生额外空白页。
12. 保存为 `.flowpdfproj`，关闭后重新打开，确认文字、格式、缩放和滚动位置仍在。
13. 选择“导出 PDF”，重新打开导出文件并搜索新增文字。

结果：`未在独立人工会话中执行 / 通过 / 失败（记录现象）`

## 场景二：中文输入法

1. 在正文中启用微软拼音或当前 Windows 中文输入法。
2. 输入拼音并选择候选词，确认候选框靠近光标且提交文字位置正确。
3. 按 Enter 新建段落，中英文混排后继续输入。
4. 输入到跨页，使用 Shift 和鼠标建立跨页选区。
5. 执行复制、剪切、粘贴、撤销和重做。
6. 在 125%、150% 和 200% Windows 显示缩放下重复光标定位检查。

结果：`自动化已验证 QInputMethodEvent 提交及缩放、滚动后的候选框变换；真实输入法候选交互待人工填写`

## 场景三：工程恢复

1. 从 PDF 导入文档模式并修改多段文字。
2. 等待状态栏显示恢复检查点已更新，或等待至少 15 秒。
3. 保存 `.flowpdfproj`，正常关闭并重新打开工程。
4. 检查文字、段落、图片、格式、光标和滚动位置。
5. 再做修改，不保存工程，使用任务管理器结束测试进程。
6. 重新启动 FlowPDF，选择“恢复”，确认来源 PDF 没有被覆盖。
7. 保存恢复后的工程并导出 PDF。

结果：`恢复服务及保存后清理已有自动测试；强制结束进程场景待人工填写`

## 场景四：复杂论文

1. 打开双栏论文，检查导入评分和检测栏数。
2. 确认软件推荐“版面编辑模式”，选择该模式并检查原版面查看功能。
3. 再次打开，强制选择文档模式。
4. 确认显示“可能改变排版”的明确警告。
5. 检查双栏内容没有被宣传为可靠还原；无法重构区域应有警告或降级说明。
6. 切回版面模式，确认先生成固定版式快照且应用不崩溃。

结果：`双栏降分和推荐逻辑已有自动测试；真实复杂论文视觉核对待人工填写`

## 退出与生命周期补充

1. 连续打开并关闭 FlowPDF 10 次。
2. 在 PDF 分析、工程保存和 PDF 导出过程中点击退出，确认任务先取消且没有 `python.exe` 残留。
3. 检查退出时没有“内存不能为 read”的 Windows 错误框。
4. 下次启动确认只清理 FlowPDF 精确登记的临时文件，不删除普通 PDF 或工程。

结果：`Qt 离屏销毁后事件队列回归与打包态退出已通过；连续 10 次真实桌面错误框检查待人工填写`
