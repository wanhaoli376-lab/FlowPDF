# Changelog

## 0.1.0a1 - Unreleased

- 建立 FlowPDF 的可启动 Qt Widgets 桌面应用骨架。
- 加入三栏界面、简体中文默认文案、国际化入口和本地滚动日志。
- 配置 pytest、ruff、PyInstaller 与 Windows 开发脚本。
- 新增可替换 PDF 后端接口与 PyMuPDF 1.28 适配器，支持渲染、文字提取、搜索、
  真删除式文字替换、图片、批注和页面操作。
- 新增统一命令栈、页面命令撤销/重做、紧凑恢复日志和安全原子保存。
- 新增统一坐标变换、内存受限 LRU 缓存、中文字体回退和文字溢出策略。
- 测试生成器可离线创建普通、中文、混合尺寸、横向、图片、扫描、300 页、
  加密及损坏 PDF。
- 接入虚拟化连续/单页查看、后台渐进渲染、高倍图块、任务取消和 512 MB LRU 缓存。
- 主窗口支持拖放打开、密码输入、最近文件、缩放/跳页、全文搜索及明暗主题。
- 页面侧栏支持多选、排序、删除、复制、旋转、插入、合并、拆分和导出。
- 增加文字、图片、常用批注、已有文字安全替换和永久擦除工具；批注可列出、跳转、
  删除并撤销。
- 增加后台应用任务、崩溃恢复入口、固定 Windows 构建基线、便携版 PyInstaller 配置
  和 300 页性能基线脚本。
- 修复 Qt 延迟销毁与后台渲染退出竞态，限制场景及缩略图中的解码图像工作集，并使线程退出
  超时可被可靠重试。
- 增加 256 MB 撤销快照预算、插入图片/PDF 的解码资源校验，以及崩溃遗留安全保存文件的
  下次启动清理登记。
- 将源 PDF 硬上限收紧为 256 MiB/25 万对象；初始查看直接复用不可变源字节，超过 64 MiB
  的原文件在创建任何撤销快照前拒绝编辑，避免大对象拒绝路径本身触发内存耗尽。
- 保留加密文档的密码保护和权限语义；页面导出要求所有者密码，并采用登记临时文件、
  重新打开验证、原子替换和下次启动清理流程。
- 新增独立于 Qt/PyMuPDF 的 `FlowDocument` 文档模型、来源映射、段落/文字样式和图片资产模型。
- 新增单栏 PDF 文档重构、标题/列表/重复页眉页脚识别、字体回退、图片排序、复杂度评分和
  “文档编辑 / 版面编辑”模式推荐对话框。
- 新增基于单一连续 `QTextDocument` 的富文本编辑器：连续输入、跨页选择、自动换行与回流、
  字符/段落格式、列表、图片、查找替换、硬分页和自动页数更新。
- 新增安全 `.flowpdfproj` 工程格式，支持图片资产、光标/选区/滚动状态，校验 Zip Slip、
  解压大小、压缩比例、版本与资产哈希。
- 新增文档模式可搜索 PDF 导出和导出后复检；中文文字层、图片、页尺寸、页数及硬分页均有
  集成测试，验证失败时不替换目标文件。
- 新增文档模式压缩恢复检查点、内容寻址图片资产、模式切换固定版式快照，以及 Story/工程
  临时文件的崩溃遗留登记清理。
- 修复后台工程保存、恢复检查点和模式快照之间的修订竞态；保存或导出期间继续输入不会再被
  静默标记为已保存或在切换模式时丢失。
- 富文本粘贴改用标签、属性和 CSS 白名单；保留文字 run 来源映射与多节结构，并修复列表标记
  重复、文档模式真实视觉缩放，以及编辑器/导出页数差异检测与切换确认。
- Windows 构建脚本兼容系统自带 PowerShell 5.1 的脚本编码规则，固定构建环境可直接完成打包。
- 文档模式新增实体分页纸张、页间阴影、当前页高亮、适应页面/宽度、滚轮缩放与滚动，以及
  防抖且只渲染可见范围的实时页缩略图；后页鼠标命中、跨页选择和 IME 坐标共用统一映射。
- 修复硬分页被重复计算而产生空白页、滚动后页码和当前页高亮不更新、缩放或滚动后中文输入法
  候选框错位、扩展出新页面后光标离开视口，以及视口边缘跨页拖选不自动滚动；新增三击选段，
  分页统计改为防抖缓存，缩略图按事件片分批绘制。
- 修复物理分页画布销毁后仍执行静态延迟回调的问题，避免连续打开/关闭编辑器时触发
  `Internal C++ object already deleted` 或 Windows 原生访问冲突。
- 修复版面模式已有文字“改一个字或删一个字”仍被紧文本框拒绝的问题：默认按真实字体试排并
  自动缩字，支持删空文字与 4pt 小字号；写入失败会恢复原文，不再留下已删除的半成品状态。
- 修复已安装字体不含目标中文字形时仍被直接用于写入的问题；文字替换现在会逐字检查字形覆盖，
  自动选择可显示全部内容的本地回退字体，找不到兼容字体时在删除原文前明确中止。
